# Hugo 博客新文章发布流程

## 一、创建新文章

### 1. 在 `content/post/` 目录下创建新的 Markdown 文件

例如：`content/post/my-new-article.md`

### 2. 添加正确的 Front Matter（文章头部信息）

```markdown
---
title: "文章标题"
date: 2026-04-24
draft: false
author: "雨落"
tags: ["标签1", "标签2"]
categories: ["分类名称"]
cover: "/images/封面图片.webp"
description: "文章简短描述"
---

# 文章正文开始

这里是文章内容...
```

### 3. Front Matter 字段说明

- **title**: 文章标题（必填）
- **date**: 发布日期，格式 YYYY-MM-DD（必填）
- **draft**: 是否为草稿，false 表示发布，true 表示草稿（必填）
- **author**: 作者名称（可选）
- **tags**: 标签数组（可选）
- **categories**: 分类数组（可选）
- **cover**: 封面图片路径，使用 `/images/xxx.webp` 格式（可选）
- **description**: 文章描述，用于 SEO 和摘要（可选）

## 二、图片资源处理

### 1. 图片存放位置

所有图片必须放在 `static/images/` 目录下

### 2. 文章中引用图片

使用相对路径：`![图片描述](/images/图片名称.webp)`

**注意事项：**

- ❌ 不要使用 Windows 绝对路径（如 `D:\hugo\...`）
- ❌ 不要使用相对路径（如 `../images/...`）
- ✅ 使用网站根路径（如 `/images/xxx.webp`）

## 三、本地预览

### 1. 构建网站

```bash
cd /d "d:\hugo\hugo_extended_0.160.1_windows-amd64\dev"
..\hugo.exe --cleanDestinationDir
```

### 2. 启动本地服务器（可选）

```bash
..\hugo.exe server
```

然后在浏览器访问 `http://localhost:1313` 预览

## 四、发布到 GitHub

### 1. 添加所有更改

```bash
cd /d "d:\hugo\hugo_extended_0.160.1_windows-amd64\dev"
git add -A
```

### 2. 提交更改

```bash
git commit -m "添加新文章：文章标题"
```

### 3. 推送到 GitHub

```bash
git push origin main
```

## 五、验证发布

### 1. 等待 GitHub Actions 构建完成

访问 https://github.com/zhuliangchun/zhuliangchun.github.io/actions
查看构建状态，确保显示绿色勾号

### 2. 访问网站验证

访问 https://zhuliangchun.github.io/
确认新文章已经显示

## 六、常见问题排查

### 问题1：文章没有显示

- 检查 `draft: false` 是否设置正确
- 检查 Front Matter 格式是否正确（必须有 `---` 分隔符）
- 检查日期格式是否正确

### 问题2：图片无法显示

- 确认图片已放在 `static/images/` 目录
- 检查图片路径是否使用 `/images/xxx.webp` 格式
- 确认图片文件名没有中文或特殊字符

### 问题3：GitHub Actions 构建失败

- 检查是否有语法错误
- 查看 Actions 日志获取详细错误信息
- 确认所有图片路径都是相对于网站根目录的

## 七、快速命令参考

```bash
# 完整发布流程（一键执行）
cd /d "d:\hugo\hugo_extended_0.160.1_windows-amd64\dev" && ^
..\hugo.exe --cleanDestinationDir && ^
git add -A && ^
git commit -m "发布新文章" && ^
git push origin main
```

## 八、文章模板

创建新文章时可以复制以下模板：

```markdown
---
title: "文章标题"
date: 2026-04-24
draft: false
author: "雨落"
tags: ["标签1", "标签2"]
categories: ["分类"]
cover: "/images/cover.webp"
description: "文章简介"
---

# 文章标题

## 第一部分

内容...

## 第二部分

内容...

## 总结

总结内容...
```

---

**提示：** 建议每次发布前先在本地预览，确认无误后再推送到 GitHub。
